---
label: second site data
hide_body: false
fields:
- name: site_name
  type: text
  config:
    required: false
  label: site name
- name: field_group
  type: blocks
  label: field group
  template_types: []
  config:
    min: 
    max: 
